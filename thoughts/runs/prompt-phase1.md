You are an agent-land session dogfooding the platform: implement GitHub issue #54 (Phase 1 — Platform Connector) end-to-end in marcoklein/agent-land. Work autonomously; never ask questions — make reasonable decisions and document them. Never merge anything.

SETUP
- git clone https://github.com/marcoklein/agent-land.git && cd agent-land && pnpm install
- Git identity: git config user.name "$GIT_USER_NAME"; git config user.email "$GIT_USER_EMAIL" (both injected in env).
- GITHUB_TOKEN is in env; gh CLI already authenticates with it (do NOT run gh auth login).
- Push with: git push https://x-access-token:${GITHUB_TOKEN}@github.com/marcoklein/agent-land.git HEAD

READ FIRST
- gh issue view 54
- docs/knowledge/multi-agent-workflow.md (Phase 1 section)
- docs/knowledge/product/features/platform-connector.md
- docs/knowledge/engine.md (Loopback section), docs/knowledge/architecture.md (composition loop)
- docs/knowledge/adrs/008 (flat JSON, no DB) — the credential store must respect it
- packages/server/src/core/session-service.ts, packages/server/src/config.ts, packages/server/src/infra/docker.ts
- packages/contracts/src/routes.ts, packages/contracts/src/session.ts
- packages/cli/src (al new/run flags, al ls rendering)
- .opencode/skills/dev-playbook/SKILL.md and .opencode/skills/okf/SKILL.md in the cloned repo — follow both.

SCOPE
1. Contracts: session create accepts `platform?: boolean` (default false) and `parentSessionId?: string`; session records/GET responses expose both.
2. Server: when platform=true, inject AGENT_LAND_URL and AGENT_LAND_BASIC_AUTH into the container env. AGENT_LAND_URL comes from server config. The injected credential is a per-session ephemeral identity: mint a random token at session create, store it in the session record (flat JSON), accept it in the server's auth path alongside the operator credential, and invalidate it when the session stops or is deleted. If HTTP auth currently terminates at nginx only, add minimal server-side auth middleware that accepts both the operator basic-auth and `session-<id>:<token>` basic-auth for the JSON/SSE API; document the tradeoff.
3. Lineage: parentSessionId stored and returned; children listed under parents.
4. CLI: `--platform` flag on al new and al run (passed through to session create); `al ls --tree` renders sessions grouped under their parent.
5. Bundled skill: agent-image/skills/agent-land-api/SKILL.md teaching a session how to create, prompt, and watch child sessions via curl against the JSON/SSE API (create session, post prompt, stream events until agent_settled, fetch result). Match the style of the other bundled skills.
6. Design note: docs/knowledge/product/designs/platform-connector-design.md per the OKF skill (status draft, generated by you, sources cited).
7. Tests: vitest coverage for credential minting/injection/revocation and lineage; `pnpm typecheck` and `pnpm test` green.

FINISH
- Branch feat/platform-connector off main; conventional commits.
- Push, `gh pr create` (title/body describe what changed + how verified; body references #54).
- `gh pr checks <n> --watch` until green; fix and re-push on red.
- `gh issue comment 54` with a summary: PR URL, key design decisions, anything you could not do.
