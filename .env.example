PORT=3000
SECRETS_DIR=./secrets
DATA_DIR=./data
AGENT_IMAGE=agent-land-pi:latest
AGE_KEY_FILE=./.age-key
OPENCODE_API_KEY=sk-xxx
DEFAULT_MODEL=deepseek-v4-pro

# Git identity injected into every agent session (leave blank to skip)
GIT_USER_NAME=
GIT_USER_EMAIL=
