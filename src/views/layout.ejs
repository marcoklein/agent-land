<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Agent Land</title>
  <link rel="stylesheet" href="/pico.min.css">
  <script src="/htmx.min.js"></script>
  <script src="/htmx-sse.js"></script>
  <style>
    .container { max-width: 900px; }
    nav { margin-bottom: var(--pico-spacing); }
    .log-container {
      background: #1e293b;
      color: #e2e8f0;
      padding: var(--pico-spacing);
      border-radius: var(--pico-border-radius);
      font-family: var(--pico-font-family-monospace);
      font-size: 0.85rem;
      max-height: 500px;
      overflow-y: auto;
      white-space: pre-wrap;
      line-height: 1.6;
    }
    .log-agent-start  { color: #60a5fa; }
    .log-turn         { color: #a78bfa; margin-top: 4px; }
    .log-tool         { color: #94a3b8; }
    .log-tool-ok      { color: #4ade80; }
    .log-tool-err     { color: #f87171; }
    .log-assistant    { color: #e2e8f0; margin: 8px 0; padding: 4px 0; border-left: 2px solid #475569; padding-left: 12px; }
    .log-user         { color: #94a3b8; margin: 4px 0; }
    .log-agent-end    { color: #60a5fa; margin-top: 8px; padding-top: 4px; border-top: 1px solid #334155; }
    .log-error        { color: #f87171; }
    .log-tool-name    { color: #fbbf24; font-weight: 600; }
    .log-pre          { color: #cbd5e1; font-size: 0.8rem; line-height: 1.4; margin: 4px 0 0 0; padding: 8px; background: #0f172a; border-radius: 4px; white-space: pre-wrap; word-break: break-all; }
    .log-truncated    { color: #64748b; font-size: 0.75rem; padding: 4px 8px; }
    .log-container details { margin: 2px 0; }
    .log-container summary { color: #94a3b8; cursor: pointer; font-size: 0.8rem; }
    .log-container button.small { font-size: 0.7rem; padding: 2px 8px; }
    .run-completed    { color: #4ade80; text-align: center; padding: 8px; border-top: 1px solid #334155; margin-top: 8px; }
  </style>
</head>
<body>
  <main class="container">
    <%- include("partials/nav") %>
    <%- include("partials/flash") %>
    <%- include(view) %>
  </main>
</body>
</html>
